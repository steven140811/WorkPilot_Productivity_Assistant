# WorkPilot 效能助手 - Windows 安装包构建指南

本文档说明如何将 WorkPilot 项目打包成 Windows 安装程序，让用户可以通过双击 `setup.exe` 进行安装，安装后直接双击桌面图标即可使用。

## 📋 前置要求

在开始打包之前，请确保您的开发环境满足以下要求：

### 必需软件

1. **Python 3.8+**
   - 下载地址: https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **Node.js 16+**
   - 下载地址: https://nodejs.org/
   - 推荐使用 LTS 版本

3. **Inno Setup 6+** (用于生成安装程序)
   - 下载地址: https://jrsoftware.org/isinfo.php
   - 安装默认选项即可

### 检查环境

打开命令提示符，运行以下命令检查环境：

```bash
python --version    # 应显示 Python 3.8.x 或更高
node --version      # 应显示 v16.x 或更高
npm --version       # 应显示 8.x 或更高
```

## 🚀 一键构建

### 方法一：使用一键构建脚本（推荐）

1. 双击运行 `build_all.bat`
2. 等待构建完成（大约需要 5-10 分钟）
3. 构建完成后会自动打开输出目录

构建产物位于 `installer\output\WorkPilot\` 目录，可以直接复制到其他电脑使用。

### 方法二：生成安装程序

在完成方法一后，如果需要生成 `.exe` 安装程序：

1. 打开 Inno Setup Compiler
2. 点击 File -> Open，选择 `build_installer.iss`
3. 点击 Build -> Compile
4. 安装程序将生成在 `installer\output\` 目录

## 📁 输出目录结构

```
installer\output\
├── WorkPilot\                    # 便携版（可直接使用）
│   ├── WorkPilot.exe            # 主程序（双击启动）
│   ├── backend\                 # 后端服务
│   │   ├── WorkPilot-Backend.exe
│   │   ├── data\               # 数据存储目录
│   │   └── ...
│   └── www\                    # 前端页面
│       ├── index.html
│       └── ...
│
└── WorkPilot-Setup-0.6.0.exe    # 安装程序（如果生成）
```

## 💻 使用说明

### 便携版使用

1. 将 `WorkPilot` 文件夹复制到目标电脑
2. 双击 `WorkPilot.exe`
3. 程序会自动启动后端服务和前端页面
4. 浏览器会自动打开应用界面

### 安装版使用

1. 双击 `WorkPilot-Setup-x.x.x.exe`
2. 按照安装向导完成安装
3. 在桌面双击 "WorkPilot 效能助手" 图标
4. 程序会自动启动并打开浏览器

## ⚙️ 配置说明

### 首次使用

1. 启动应用后，点击顶部的 "⚙️ 设置" 标签
2. 配置 LLM API：
   - **API URL**: 填写您的 LLM API 地址
   - **API Key**: 填写您的 API 密钥
   - **模型名称**: 填写要使用的模型名称
3. 点击 "测试连接" 验证配置
4. 点击 "保存配置"

### 数据存储

- 所有数据存储在 `backend\data\reports.db` 文件中
- 备份此文件即可备份所有数据
- 迁移到新电脑时，复制整个 `WorkPilot` 文件夹即可

## 🔧 故障排除

### 常见问题

**Q: 双击 WorkPilot.exe 后无反应**
- 检查是否有杀毒软件拦截
- 尝试右键 -> 以管理员身份运行

**Q: 浏览器打开后显示无法连接**
- 等待几秒钟让后端服务完全启动
- 检查防火墙是否阻止了端口 5000 和 3000

**Q: 生成周报时报错**
- 检查设置页面中的 LLM 配置是否正确
- 确认 API Key 是否有效

### 端口占用

如果端口被占用，可以修改 `WorkPilot.py` 中的端口设置：

```python
BACKEND_PORT = 5000   # 后端端口
FRONTEND_PORT = 3000  # 前端端口
```

## 📝 开发者说明

### 手动构建步骤

如果自动构建脚本失败，可以手动执行以下步骤：

```bash
# 1. 安装后端依赖
cd backend
pip install -r requirements.txt
pip install pyinstaller

# 2. 构建前端
cd ../frontend
npm install
set REACT_APP_API_URL=http://localhost:5000
npm run build

# 3. 打包后端
cd ..
pyinstaller --onedir --console --name=WorkPilot-Backend backend/app.py

# 4. 打包启动器
pyinstaller --onedir --windowed --name=WorkPilot WorkPilot.py

# 5. 组装目录结构
mkdir installer\output\WorkPilot
xcopy dist\WorkPilot\* installer\output\WorkPilot\ /E
mkdir installer\output\WorkPilot\backend
xcopy dist\WorkPilot-Backend\* installer\output\WorkPilot\backend\ /E
mkdir installer\output\WorkPilot\www
xcopy frontend\build\* installer\output\WorkPilot\www\ /E
```

### 自定义图标

1. 准备一个 `.ico` 格式的图标文件
2. 在 `build_all.bat` 中的 pyinstaller 命令添加 `--icon=your_icon.ico`
3. 在 `build_installer.iss` 中设置 `SetupIconFile=your_icon.ico`

## 📄 许可证

本项目采用 MIT 许可证。

## 🤝 支持

如有问题，请在 GitHub 上提交 Issue：
https://github.com/steven140811/Weekly-Report-and-OKR-Assistant/issues
